from math import sqrt, ceil, floor
import random

# numero = int(input('Digite um número: '))
# raiz_quadrada = sqrt(numero)
# print('A raiz_quadrada de {} arredondando para cima (math.ceil) fica: {} arredondando para baixo (math.floor) fica: {}'.format(numero, ceil(raiz_quadrada), floor(raiz_quadrada)))

print(random.random(),'\n',random.randint(1, 25))